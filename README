Gabriel Vigil
3/9/2022

Uses node version 14.15.5

Stack used from Requirements:
React 17
React-Redux
Redux-Saga
React Router
Material-UI
Node
Express
Postgres
REST

To Build:
npm run Build

To Run:
npm run start

To Run the Database:
Make sure you have docker installed 

docker run --name postgres-db -e POSTGRES_PASSWORD=mysecretpassword -p 5432:5432 -d postgres

Future todo:
Add schema checking on routes
Add a front end model api system
Abstract react code to multiple components
Add proper error handling and logging on the server side end points 
fix highlighting on frontend list
add loading screen
Fix edit/add item modal
add purchased/checked to the database